// First request to a general endpoint within limits should not be rate limited
// (gets 401 because of invalid token — but NOT 429)
get("$baseUrl/api/v1/auth/me", {
  headers: { Authorization: "Bearer invalid" }
})
.expect(status: 401)
